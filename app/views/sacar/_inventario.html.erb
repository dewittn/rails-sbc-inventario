	  <tr class="<%= cycle :odd,:even %>">
		<td><%=h inventario.id %></td>						
	    <td><%=h inventario.fila %></td>
	    <td><%=h inventario.columna %></td>
	    <td><%=h inventario.cantidad %><%= cantidad_por_sacar(inventario) %></td>	
		<%= detalles(inventario) %>		
	    <td><%= link_to 'Devolver', edit_sacar_path(inventario) %></td>
	    <td><%= link_to 'Reinventariar', sacar_path(inventario), :method => 'put' %></td>
	    <td><%= link_to 'Eliminar', sacar_path(inventario), :confirm => "Est&aacute;s seguro?", :method => 'delete' %></td>
	  </tr>